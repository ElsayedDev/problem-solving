#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    map<string, vector<string>> values = {
        {"s", {"a", "c"}},
        {"a", {"b", "f"}},
        {"b", {}},
        {"c", {"b", "d"}},
        {"d", {"f"}},
    };
    

}
